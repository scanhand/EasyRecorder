<UserControl x:Class="AMK.UI.RecorderItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:amk="clr-namespace:AMK"
             xmlns:local="clr-namespace:AMK.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ListView Name="RecoderListView">
            <ListView.View>
                <GridView>
                    <!-- Item -->
                    <GridViewColumn Width="200">
                        <GridViewColumnHeader>Item</GridViewColumnHeader>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <Image Source="{Binding ImageSource}" Width="16"/>
                                    <TextBlock Text="{Binding Recorder}"/>
                                </DockPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Description -->
                    <GridViewColumn Width="200">
                        <GridViewColumnHeader>Description</GridViewColumnHeader>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <!--<Image Source="/AutoMouseKeyboard;component/icons8-record-64.ico" Width="16"/>-->
                                    <TextBlock Text="{Binding Description}"/>
                                </DockPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <!-- Etc -->
                    <GridViewColumn Width="200">
                        <GridViewColumnHeader>Etc</GridViewColumnHeader>
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <DockPanel>
                                    <Image Source="/AutoMouseKeyboard;component/Resources/icons8-cursor-64.png" Width="16"/>
                                    <TextBlock Text="{Binding Point}"/>
                                </DockPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
